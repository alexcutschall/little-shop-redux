<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="../../../reset.css">
    <link rel="stylesheet" type="text/css" href="../../../main.css">
    <title>Edit Item</title>
  </head>

  <body>
    <%= erb :navigation %>

    <header class="header">
      <a href='/items-dashboard'>Dashboard</a>
      <h1 class="header-title">Items</h1>
      <a href='/items/new'>Create A New Item</a>
    </header>
<section class="merchant-section">
    <% @items.each do |item| %>
    <div class="item-display">
      <img src="<%= item.image %>" alt="Cotton Candy" class="item-image"/>
      <a href='/items/<%= item.id %>'><%= item.title %></a>
      $<%= item.price.to_f / 100 %>
    </div>
    <% end %>
  </section>
  </body>
</html>
